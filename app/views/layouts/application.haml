!!!
%html
  %head
    %meta{content: 'text/html; charset=UTF-8', 'http-equiv' => 'Content-Type'}/
    %meta{name: 'viewport', content: 'width=device-width, initial-scale=1'}
    %title= yield(:title) || 'SpeedRunRacing!'
    = stylesheet_link_tag 'application.css'
    = javascript_include_tag 'application.js', defer: Rails.env.production?
    = javascript_pack_tag 'application.js', defer: Rails.env.production?
    = csrf_meta_tags
    = render partial: 'shared/favicons'
    = content_for(:html_head)
  %body
    = Gon::Base.render_data
    = render 'layouts/header'
    .container
      = render partial: 'shared/alerts'
      = yield
