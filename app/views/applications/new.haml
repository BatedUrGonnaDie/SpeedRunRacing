= provide(:title, 'New Application')
- if current_user.nil?
  %p You need to be signed in to view this page.
- else
  .card
    .card-header.text-white Hook into SpeedRunRacing
    .card-body
      %p Create a new developer application here. This will let users authorize you to do things for them.
      %p
        = form_for(Doorkeeper::Application.new, url: applications_path) do |f|
          .form-group
            %p
              .row
                .col-lg-8
                  = f.text_field(:name, class: 'form-control', placeholder: 'Application name (what users will see)')

            %p
              .row
                .col-lg-8
                  = f.text_field(:redirect_uri, class: 'form-control', placeholder: 'Redirect URI (include http:// or https://)')
                  If you haven't created a page for Splits I/O to redirect users back to yet, put "debug" here.

            %p
              .row
                .col-lg-8
                  .input-group-btn
                    = f.submit 'Create', class: 'btn btn-success'
